# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'chargingSpeed' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for chargingSpeed
  pod 'AppCenter'

  target 'chargingSpeedTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'chargingSpeedUITests' do
    # Pods for testing
  end

end
